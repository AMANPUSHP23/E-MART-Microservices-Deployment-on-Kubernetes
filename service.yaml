apiVersion: v1
kind: Service
metadata:
  name: docker-app
  labels:
    app: docker-app
spec:
  type: LoadBalancer
  selector:
    app: docker-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
